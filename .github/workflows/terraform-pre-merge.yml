name: 'Terraform Pre-Merge'

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  id-token: write
  contents: read

jobs:
  terraform-pre-merge:
    name: 'Terraform Pre-Merge'
    runs-on: ubuntu-latest
    environment: production

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Apply IAM
        run: terraform apply -target=aws_iam_openid_connect_provider.github_oidc -auto-approve

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
         role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/GitHubActionsEKSRole
         aws-region: us-east-1

      - name: Terraform Plan (Pre-Merge)
        run: terraform plan -input=false